# ===========================================
# NOVAI Web - Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control
# ===========================================

# -----------------
# API URL (Required)
# -----------------
# Must include /api suffix since backend uses global prefix
# Local development: http://localhost:3001/api
# Production: https://your-api-domain.com/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# -----------------
# Local Network IP (Optional - for Phone Scan feature)
# -----------------
# Set this to your computer's local network IP for the Phone Scan QR code
# to work properly when testing on a phone on the same network.
# Find your IP: macOS/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
#               Windows: ipconfig | findstr /i "IPv4"
# Example: 192.168.1.100
# NEXT_PUBLIC_LOCAL_IP=192.168.1.100

# -----------------
# Supabase (Required for client-side storage access)
# -----------------
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# Note: Use the anon/public key here, NOT the service role key
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
